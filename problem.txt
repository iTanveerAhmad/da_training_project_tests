Create a Dockerized project that clones the Docker workshop repository https://github.com/docker/getting-started-app during build time, installs pinned dependencies using npm ci, and starts the app on port 3000.
